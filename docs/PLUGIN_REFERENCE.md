# Plugin Reference

This document explains how `naval` is structured and why it is both a plugin and a skill pack.

## Short Answer

| Question | Answer |
|---|---|
| Is `naval` a plugin? | Yes. `.codex-plugin/plugin.json` makes the repository installable as a Codex plugin. |
| Is it also skills? | Yes. The plugin exposes 76 `skills/n-*` folders, each with its own `SKILL.md`. |
| Why both? | The plugin gives packaging, metadata, versioning, and marketplace visibility. Skills give small callable behaviors that search well and load only the relevant instructions. |
| Why `n-`? | A short namespace makes the pack easy to find in skill search and avoids generic names like `wealth` or `reading`. |

## Architecture

```text
User prompt
  -> search for n- or call n-router
  -> selected skills/n-*/SKILL.md
  -> targeted references from references/
  -> scorecard, decision, practice, experiment, review, or reading path
```

## Repository Map

| Path | Purpose | Edited By |
|---|---|---|
| `.codex-plugin/plugin.json` | Plugin identity, display metadata, and skill root. | Human or plugin tooling. |
| `skills/n-*/SKILL.md` | Callable skill instructions. | Generated by `scripts/build_naval_pack.py`. |
| `skills/n-*/agents/openai.yaml` | Agent metadata for each skill. | Generated by `scripts/build_naval_pack.py`. |
| `references/skill-catalog.md` | Human-readable inventory of all skills. | Generated. |
| `references/router-guide.md` | Routing rules for vague prompts. | Generated. |
| `references/coverage-matrix.yaml` | Maps book sections to skills and references. | Generated. |
| `references/chapter-summaries/` | Paraphrased coverage anchors by major area. | Generated. |
| `references/workflows/` | Reusable scorecards and protocols. | Generated. |
| `scripts/install_local.py` | Marketplace registration and symlink installer. | Hand-maintained. |
| `scripts/validate_public.py` | Public repo structural validation. | Hand-maintained. |
| `scripts/check_coverage.py` | Coverage validation. | Generated. |

## Plugin Versus Direct Symlink

| Approach | Best For | Tradeoff |
|---|---|---|
| Codex plugin | Codex UI visibility, plugin metadata, version-aware refresh, clean packaging. | Requires a plugin-aware host. |
| Direct symlink | Agents that scan `SKILL.md` folders directly. | No plugin metadata or marketplace surface. |
| Both | Local multi-agent setups. | Slightly more setup, but one repo stays the source of truth. |

## Skill Design Rules

| Rule | Why It Exists |
|---|---|
| One narrow job per skill | Smaller skills route better and avoid loading unrelated context. |
| Route detailed knowledge to `references/` | Keeps `SKILL.md` lean and inspectable. |
| End in an output | The skill should produce a decision, scorecard, practice, experiment, boundary, removal, or reading path. |
| Use `n-router` for ambiguity | Vague prompts should not guess silently when routing can be made explicit. |
| Keep source fidelity visible | The pack is public and paraphrased; exact quotes need extra care. |
| Validate coverage | A book-derived pack should make omissions detectable. |

## Output Types

| Output Type | Skills That Commonly Produce It |
|---|---|
| Decision | `n-decision-rules`, `n-big-life-decisions`, `n-accept-change-leave` |
| Scorecard | `n-opportunity-scorecard`, `n-relationship-scorecard`, `n-wealth-map` |
| Experiment | `n-principle-to-action`, `n-specific-knowledge`, `n-leverage-stack` |
| Practice | `n-meditation-system`, `n-presence-practice`, `n-happiness-habits` |
| Boundary | `n-freedom-from-expectations`, `n-values-filter`, `n-risk-of-ruin` |
| Removal | `n-modern-addiction-defense`, `n-time-value-focus`, `n-desire-audit` |
| Reading path | `n-reading-curriculum`, `n-reading-system`, `n-next-sources` |
| Review | `n-daily-review`, `n-weekly-compound-review`, `n-coverage-auditor` |

## Quality Checks

| Check | Command | Catches |
|---|---|---|
| Public structure | `python3 scripts/validate_public.py` | Missing docs, manifest issues, missing skill metadata, broken skill references. |
| Coverage | `python3 scripts/check_coverage.py` | Missing skill coverage, invalid mapped references, unmapped `n-*` skills. |
| Plugin schema | `python3 ~/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py .` | Codex plugin manifest problems. |
| Skill schema | `python3 ~/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/n-router` | Individual skill frontmatter and structure issues. |

## How To Add A Skill

1. Add the skill definition to `SKILLS` in `scripts/build_naval_pack.py`.
2. Add or update coverage in the script's coverage mapping.
3. Regenerate:

```bash
python3 scripts/build_naval_pack.py
```

4. Validate:

```bash
python3 scripts/validate_public.py
python3 scripts/check_coverage.py
```

5. Inspect the generated `skills/n-new-skill/SKILL.md` before committing.

## Source Boundary

The plugin should express operational knowledge in original wording. It should not include the full book text, long excerpts, private notes, or unverifiable exact quotes. Use `n-source-fidelity` and `n-quote-safety` whenever exact phrasing or publication is involved.
