interface:
  display_name: "N Inversion Filter"
  short_description: "N Inversion Filter: Find what will fail, what to avoid, and"
  default_prompt: "Use $n-inversion-filter to find what will fail, what to avoid, and which mistakes matter most."
