interface:
  display_name: "N Decision Rules"
  short_description: "N Decision Rules: Apply Naval's major decision heuristics to"
  default_prompt: "Use $n-decision-rules to apply Naval's major decision heuristics to hard choices."
