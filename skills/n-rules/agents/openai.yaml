interface:
  display_name: "N Rules"
  short_description: "N Rules: Apply compact Naval rules and maxims as reminders, not"
  default_prompt: "Use $n-rules to apply compact Naval rules and maxims as reminders, not substitutes for reasoning."
